"""
(1) 変数priceの値に1.1を掛けた値は300000以下か

(2) x(=記号1つは代入演算子を意味する。等しいかどうかを判定するには、比較演算子の==を使う)

(3) 変数kansaiに「gihu」は含まれるか

(4) 変数aとbの合計が60よりも大きく、かつ変数dayの値は3と等しいか

(5) Falseかどうか(条件式はbool型に評価されるので、Falseそのものを記述できる。この条件式は常にelseブロックのみを実行する)
"""